小马绘画 v2.1：按 L0-L3 四层重制，新增完整绘画流程与角色库。

一键下载：`mlp-g4-official-style_v2.1.zip`，解压到 `%USERPROFILE%\.codex\skills\`（其他平台 `~/.codex/skills/`），覆盖同名 `mlp-g4-official-style` 目录即为升级。

（GitHub 的 Release 资产名不支持中文，本地打包名 `小马绘画_v2.1_覆盖安装版.zip` 会被服务端丢弃中文字符，因此资产使用 ASCII 名。）

### 本版变化

- 结构重制为四层：**L0 小马基本形体** / **L1 绘画基本流程** / **L2 画风模块** / **L3 角色库**；验收优先级 P0/P1/P2 保持不变，层号改用 L 前缀以示区分。
- 新增 L1 绘画基本流程：需求取材 → 构图与镜头 → 骨架 → 体块 → 结构自检 Gate → 线稿与光影 → 细节 → 出图后验收，每一步都写明先定什么、什么算没定完。
- 光影细化：主光方向唯一、明暗交界线随体块、投影方向与落地形状、环境光抬暗部、色温统一、边缘光、鬃毛/金属/布料/蹄的材质逻辑、分级与笔触由画风决定。
- 新增前蹄专条与飞马双翼专检：规则写在 L0，L1 在骨架阶段与结构 Gate 各设一个检查点，避免同一句话两处维护。
- 新增生成提示词骨架、负向词表、固定 4 行自检输出（计数 / 身份 / 结构 / 判定）。
- 新增角色库 8 位：柔柔、云宝、紫悦、珍奇、苹果嘉儿、碧琪、塞拉斯蒂娅、露娜；每条含身份常量、造型气质、常见错法，以及按画风划分的子条目。
- 原 `g4-official-style.md` 与原 `pegasus-anatomy.md` 合并为 `references/body-base.md`；原 `reference-hygiene.md` 并入流程的参考图状态一节。
- 敦煌模块精简为纯画风语言，角色适配标签移入 L3 对应条目；画风模块要求互相独立、不混搭、不覆盖形体规则。
- `SKILL.md` 收敛为入口层：8661B → 3651B，只留优先级、分层路由、读取纪律、画风隔离、生成前检查、失败处理。

### 校验

- `quick_validate.py` 通过。
- 发布包内所有文件 sha256 与工作区源码一致，包内根目录为 `mlp-g4-official-style/`（保证覆盖安装）。
